
public enum FaceDir
{
    up,
    right,
    down,
    left
}

public enum EnemyState{
    Move,
    Alert,
    ResetView,
    Found
}

public enum InteractType{
    Hide,
    Sleep,
    Null            //表示当前按“E”无效
}